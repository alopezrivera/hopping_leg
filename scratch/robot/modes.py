class walking:

    gains = {
        asd
    }

    def activation(state):
        process(state)
        return bool


---


modes = [walking, ...]

call(*modes)

---

for mode in modes:
    transition_mode{n} = mode.activation

---

GUI

gain_mode{n} = mode.gains -> GUI -> modify -> mode.gains = modified_gains